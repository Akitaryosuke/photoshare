<h1>一覧</h1>
<div>
  <%= link_to "投稿", new_post_path %>
</div>

<% @posts.each do |post| %>
  <div>
    <div>
      <%= post.user.name %>
    </div>
    <% post.image.each do |image| %>
      <%= image_tag image.variant(resize: '500x500') %>
    <% end %>
  </div>
<% end %>